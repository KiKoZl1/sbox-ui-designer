@namespace SboxUiDesigner.Runtime
@using Sandbox;
@using Sandbox.UI;
@inherits PanelComponent

@* Generic host PanelComponent. SuiPanel<TView> adds the rendered Panel as a
   child of this.Panel at runtime — this template provides the empty <root>
   the ScreenPanel needs to anchor against. *@

<root style="width: 100%; height: 100%;"></root>
