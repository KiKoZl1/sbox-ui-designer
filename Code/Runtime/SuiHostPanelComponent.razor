@namespace SboxUiDesigner.Runtime
@using Sandbox;
@using Sandbox.UI;
@inherits PanelComponent

@* Generic host PanelComponent — empty root.
   SuiPanel<TView> adds the rendered Panel as a child of this.Panel at runtime.
   Has no markup of its own and never appears in the Asset Browser. *@

<root></root>
