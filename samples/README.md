# Samples

Sample `.sui` documents used for manual testing and golden-file generator tests.

Files (planned):

- `ui/simple_panel.sui`
- `ui/inventory_basic.sui`
- `ui/hotbar_basic.sui`
- `ui/hud_survival.sui`

Each sample may include the expected `.razor` / `.razor.scss` output as a golden-file test fixture.
